# Prevent npm from outputting warnings to stderr (which cPanel interprets as errors)
loglevel=error
engine-strict=false

# cPanel "Run NPM Install" sets NODE_ENV=production and would skip devDependencies.
# Keep @types/* and typescript available if you ever run `npm run build` on the server.
production=false
omit=
